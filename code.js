initialize class bankAcc
constructor properties (name, id, address, pin, acctype, accCreatedDate)

savingsAcc(){
userobject = new bankAcc(
this.name
this.id
this.address
this.acctype
this.pin
)

fs.writeFile(
if accholders info is saved, then print acc created
)
}
group 3